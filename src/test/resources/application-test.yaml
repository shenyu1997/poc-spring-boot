logging.level:
  org.hibernate:
    SQL: DEBUG
    type.descriptor.sql.BasicBinder: TRACE
  yu.shen.poc.common.rest: DEBUG

spring:
  jpa:
    show-sql: true
    hibernate.ddl-auto: create-drop
  datasource:
    generate-unique-name: true